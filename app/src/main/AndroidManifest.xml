<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.reclaim.reclaim">

    <!-- ============================================================
         Application-level configuration
         ============================================================ -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reclaim"
        android:name="com.reclaim.reclaim.ReclaimApplication">

        <!-- ============================================================
             Main Activity (entry point)
             ============================================================ -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            android:authorities="com.reclaim.reclaim.fileprovider">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />

        </provider>
        <activity
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reclaim.Splash"
            android:name="com.reclaim.reclaim.MainActivity">

            <!-- Launcher intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================
             (Optional) Other activities/fragments/services go here
             ============================================================ -->
        <!-- Example:
        <activity android:name=".ui.trigger.TriggerActivity" />
        -->

    </application>
</manifest>